# Annotation Task Prompt

You will annotate a learner transcription using the provided guidelines and JSON template.

Requirements:
- Extract each learner utterance in order.
- Fill all required keys in the JSON template.
- Use only allowed values listed in guidelines.
- Preserve uncertain content with comments instead of deleting data.
- Return JSON only.
